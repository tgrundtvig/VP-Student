package dk.viprogram.week01;

/**
 * Utility class for combat calculations.
 * Practices methods and simple logic.
 */
public class CombatCalculator {

    // TODO: Create a method calculateDamage(int attackPower, int defense) that:
    // - Returns the damage dealt (attackPower - defense)
    // - Minimum damage is 1 (even if defense >= attackPower)
    // - Both attackPower and defense must be >= 0


    // TODO: Create a method isCriticalHit(int attackRoll) that:
    // - Returns true if attackRoll >= 90 (representing a 10% chance if roll is 0-99)
    // - Returns false otherwise


    // TODO: Create a method calculateCriticalDamage(int baseDamage) that:
    // - Returns baseDamage * 2 (critical hits deal double damage)


    // TODO: Create a method canEvade(int defenseRoll) that:
    // - Returns true if defenseRoll >= 85 (representing a 15% chance if roll is 0-99)
    // - Returns false otherwise


    // TODO: Create a method calculateFinalDamage(int attackPower, int defense, int attackRoll, int defenseRoll) that:
    // - Returns 0 if the attack is evaded
    // - Returns critical damage if it's a critical hit
    // - Returns normal damage otherwise
    // This combines all the previous methods


}
